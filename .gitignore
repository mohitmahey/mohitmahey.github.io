# This repository holds the built site only. Everything in it is generated by
# the builder one folder up and replaced on every build; nothing here is
# hand-edited. Keep this list short — anything the site needs must be tracked.
.DS_Store
